# YOLOv8 Dataset Configuration
# Dataset: Circle Detection Dataset
# Classes: 6 categories (blue_stack, blue_space, green_stack, green_space, red_stack, red_space)

# Dataset paths
path: /home/zhaocake/WorkSpace/Vision/Gongxun_Model_Train/circle_datasets  # 数据集根目录
train: train/images  # 训练图像目录 (相对于path)
val: val/images      # 验证图像目录 (相对于path)

# 类别数量
nc: 6

# 类别名称 (按类别ID排序: 0, 1, 2, 3, 4, 5)
names:
  0: blue_stack
  1: blue_space
  2: green_stack
  3: green_space
  4: red_stack
  5: red_space
